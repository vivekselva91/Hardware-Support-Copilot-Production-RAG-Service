services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      # Uncomment to enable real LLM generation:
      # - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
      - APP_ENV=docker
      - RATE_LIMIT_PER_MIN=60

  ui:
    build: .
    command: streamlit run ui.py --server.port 8501 --server.address 0.0.0.0
    ports:
      - "8501:8501"
    environment:
      - API_URL=http://api:8000
    depends_on:
      - api
